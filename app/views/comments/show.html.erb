<h2>Website: <em>
  <a href="<%= @comment.body %>"><%= @comment.body %></a> </em> </h2>

<%= link_to 'Edit', "/comments/#{@comment.id}/edit" %>
|
<%= link_to 'Delete', "/comments/#{@comment.id}/delete" %>
|
<%= link_to 'Back', "/comments" %>

<p>user_id: <%= @comment.user_id %></p>
<p>review_id: <%= @comment.review_id %></p>
