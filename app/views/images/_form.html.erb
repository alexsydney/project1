<hr>
<%= form_for [@restaurant, @image], html: {multipart: true } do |f| %>
  <fieldset>
    <%= f.label :title %>
    <%= f.text_field :title %> <!-- give us  -->
  </fieldset>
  <fieldset>
    <%= f.label :description %>
    <%= f.text_field :description %> <!-- give us  -->
  </fieldset>

  <fieldset>
    <%= f.label :image_url %>
    <%= f.cl_image_upload :image_url %> <!-- give us  -->
  </fieldset>


  <%= f.submit 'Create', class:"btn btn-primary" %>
<% end %>
