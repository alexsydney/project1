
<%= form_for @image do |f| %>
  <fieldset>
    <%= f.label :title %>
    <%= f.text_field :title %> <!-- give us  -->
  </fieldset>
  <fieldset>
    <%= f.label :description %>
    <%= f.text_field :description %> <!-- give us  -->
  </fieldset>
  <fieldset>
    <%= f.label :restaurant_id %>
    <%= f.text_field :restaurant_id %> <!-- give us  -->
  </fieldset>
  <fieldset>
    <%= f.label :image_url %>
    <%= f.text_field :image_url %> <!-- give us  -->
  </fieldset>
  <fieldset>
    <%= f.label :user_id %>
    <%= f.text_field :user_id %> <!-- give us  -->
  </fieldset>

  <%= f.submit 'Create' %>
<% end %>
